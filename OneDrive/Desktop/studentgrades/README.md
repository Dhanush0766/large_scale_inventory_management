# studentgrades

A simple Java project to manage student grades.

## How to run
1. Compile the project: `mvn compile`
2. Run the application: `java -cp target/classes com.studentgrades.App`

Or use the VS Code task "Run App".
